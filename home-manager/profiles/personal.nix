{...}:
{
  custom = {
    hyprland = {
      enable = true;

      hyprpaper.enable = true;

      sensitivity = -0.5;

      keybinds = {
        launchers.enable = true;
        media.enable = true;
        mouse.enable = true;
        window.enable = true;
        workspace.enable = true;
      };
    };

    cursor = {
      enable = true;
    };
  };
}
